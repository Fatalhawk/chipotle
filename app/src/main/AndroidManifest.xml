<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sensei.companion">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".display.AppLauncher"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="landscape">
        </activity>

        <service
            android:name=".connection.ConnectService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".display.TouchBarActivity" />
        <activity android:name=".display.TrackpadActivity" />
        <activity android:name=".display.PcManager" />
        <activity android:name=".display.GestureActivity" />
        <activity android:name=".display.PcSelectionActivity" />
        <activity android:name=".display.user_interface.UIAppLauncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>